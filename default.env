# ------------------------------
# eXtremeSHOK.com Docker Webserver Configuration
# ------------------------------
# Your timezone
TIMEZONE=UTC

# Hostname (fqdn)
FQDN_HOSTNAME=$(hostname -f)

# Email address used to enable letsencrypt
ACME_EMAIL=admin@extremeshok.com

# Fixed project name
COMPOSE_PROJECT_NAME=xs

# Fix for unknown PWD
PWD=${PWD:-/datastore}

# Disable IPv6
# network will be created IPv6 enabled, all containers will be created without IPv6 support.
# Use 1 for disabled, 0 for enabled
SYSCTL_IPV6_DISABLED=0

# OpenLiteSpeed Docker Image Selection
# Use official LiteSpeed image for multi-PHP support
# Options: 
#   - litespeedtech/openlitespeed (official, supports multi-PHP)
#   - extremeshok/openlitespeed-php (optimized, single PHP version)
OPENLITESPEED_IMAGE=litespeedtech/openlitespeed

# OpenLiteSpeed Image Tag
# For litespeedtech/openlitespeed: Use versioned tags like 1.8.4-lsphp{VERSION}
# For extremeshok/openlitespeed-php: latest or 20.04
# Leave empty to auto-generate from PHP_VERSION (will use latest stable version)
# Note: latest-lsphp{VERSION} format is not available, use specific versions
OPENLITESPEED_TAG=

# PHP Version
# Specify the PHP version to use with OpenLiteSpeed
# Available options: 74 (PHP 7.4), 80 (PHP 8.0), 81 (PHP 8.1), 82 (PHP 8.2), 83 (PHP 8.3), 84 (PHP 8.4)
# Default: 74 (PHP 7.4)
# This is used to construct the image tag automatically when OPENLITESPEED_TAG is empty
PHP_VERSION=74
